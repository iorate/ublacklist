name: Build

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - uses: actions/setup-node@v1.1.0
      with:
        version: 13.x
    
    - name: Install dependencies
      run: PUPPETEER_SKIP_CHROMIUM_DOWNLOAD npm install

    - name: Test
      run: npm test

    - name: Build
      run: npm run build
