---
ublacklist: patch
---

Fixed an issue where the popup stayed blank on browser internal pages in Edge. The popup now shows an empty dialog on pages that cannot be blocked, such as browser internal pages, instead of a "Block this site" dialog with a disabled button.
