export const permissionExemptOrigins = [
  "https://raw.githubusercontent.com",
  "https://cdn.statically.io",
] as const;

export const subscriptionURLOrigin = "https://ublacklist.github.io";
export const rulesetSubscriptionURL = `${subscriptionURLOrigin}/rulesets/subscribe`;
export const serpinfoSubscriptionURL = `${subscriptionURLOrigin}/serpinfo/subscribe`;
